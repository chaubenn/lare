import { Landing } from "@/components/landing";

/**
 * The whole website. Lare is the desktop app and the Chrome extension; the site exists to explain
 * that and hand over the downloads. Everything that used to live here — the feed, drafts, sessions,
 * profiles, post pages — was a second copy of an app that people use from the app.
 */
export default function HomePage() {
  return <Landing />;
}
