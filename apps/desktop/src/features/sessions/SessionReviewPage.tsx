import { PageHeader } from "@/components/ui/Card";

/** Placeholder — replaced by the interview review UI. */
export function SessionReviewPage() {
  return <PageHeader title="Session" subtitle="Loading…" />;
}
