import { PageHeader } from "@/components/ui/Card";

/** Placeholder — replaced by the studio editor. */
export function StudioEditorPage() {
  return <PageHeader title="Studio" subtitle="Loading…" />;
}
