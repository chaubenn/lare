import { PageHeader } from "@/components/ui/Card";

/** Placeholder — replaced by the full recordings manager. */
export function RecordingsPage() {
  return <PageHeader title="Recordings" subtitle="Loading…" />;
}
